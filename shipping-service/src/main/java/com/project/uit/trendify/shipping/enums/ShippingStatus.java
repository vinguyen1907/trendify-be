package com.project.uit.trendify.shipping.enums;

public enum ShippingStatus {
    PENDING,
    PROCESSING,
    SHIPPED,
    DELIVERED,
    CANCELLED,
    RETURNED,
    REFUNDED,
    ON_HOLD,
    OUT_FOR_DELIVERY,
    FAILED_DELIVERY;
}
